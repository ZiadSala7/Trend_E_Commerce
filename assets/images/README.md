# App logo

Place your app icon image here as **`appLogo.png`** (recommended: 1024×1024 px for best results).

Then run to generate launcher icons for Android, iOS, and web:

```bash
dart run flutter_launcher_icons
```
